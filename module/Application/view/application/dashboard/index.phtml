<?php
/**
 * @var Laminas\View\Renderer\PhpRenderer $this
 * @var array|null $usuario
 */
$this->headTitle('Dashboard');
?>
<div class="max-w-3xl">
    <h1 class="text-2xl font-semibold text-gray-900">
        Olá, <?= $this->escapeHtml($usuario['nome'] ?? $usuario['login'] ?? 'usuário') ?>!
    </h1>
    <p class="mt-2 text-sm text-gray-600">
        Esta é a tela inicial de exemplo do esqueleto. Substitua este conteúdo
        pelas telas e regra de negócio do sistema real — o layout (navbar +
        sidebar) em <code class="text-gray-800">module/Application/view/layout/layout.phtml</code>
        continua valendo para qualquer tela nova.
    </p>

    <div class="mt-6 rounded-lg border border-gray-200 bg-white p-6 shadow-sm">
        <h2 class="text-sm font-semibold uppercase tracking-wide text-gray-500">Próximo passo</h2>
        <p class="mt-2 text-sm text-gray-700">
            Veja o módulo <code class="text-gray-800">Modelo</code>
            (<a class="text-blue-600 hover:underline" href="<?= $this->url('modelo') ?>">/modelo</a>)
            para o padrão de Controller + Entity + Repository + Form + View a
            ser copiado ao adicionar a regra de negócio real.
        </p>
    </div>
</div>
